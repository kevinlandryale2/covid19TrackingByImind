


/*
Future<Null> parse() async{
   RssFeed _feed;
    RssFeed recu= await chargerRss();
    if(recu!=null){
      _feed = recu;
      print(_feed.items.length);

      _feed.items.forEach((feedItem){
        RssItem item= feedItem;

            print("Titre: "+item.title);
            print("Description: " +item.description);
            print("Auteur: " +item.author);
            print("Date publication: " +item.pubDate);
            print("Image: " +item.enclosure.url);
      });
      print(_feed.items.length);
    }
 }

 */